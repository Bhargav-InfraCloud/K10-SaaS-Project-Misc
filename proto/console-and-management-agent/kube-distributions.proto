syntax = "proto3";

package kubedist;


enum KubeDistribution {
    Unknown = 0;
    AWS = 1;
    Azure = 2;
    OpenShift = 3;
    Google = 4;
    DigitalOcean = 5;
    VSphere = 6;
    SUSERancher = 7;
    OnPremise = 8;
}

// OnPremise might have options: minikube, kind, kubeadm, k3s, etc.

// Skip for now.